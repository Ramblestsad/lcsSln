{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build Todo.WebApi",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "/property:GenerateFullPaths=true"
            ],
            "problemMatcher": "$msCompile",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "label": "run Todo.WebApi",
            "command": "dotnet",
            "type": "process",
            "args": ["run", "--launch-profile", "Local"],
            "problemMatcher": "$msCompile",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "label": "watch Todo.WebApi",
            "command": "dotnet",
            "type": "process",
            "args": ["watch", "run", "--launch-profile", "Local"],
            "problemMatcher": "$msCompile",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}
