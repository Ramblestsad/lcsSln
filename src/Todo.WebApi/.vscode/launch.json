{
    "version": "0.2.0",
    "configurations": [
        {
            "name": ".Net Launch (Todo.WebApi)",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build Todo.WebApi",
            "program": "${workspaceFolder}/bin/Debug/net10.0/Todo.WebApi.dll",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "serverReadyAction": {
                "action": "openExternally",
                "pattern": "\\bNow listening on:\\s+(https?://\\S+)"
            },
            "env": {
                "ASPNETCORE_ENVIRONMENT": "Development"
            }
        },
        {
            "name": ".NET Core Attach",
            "type": "coreclr",
            "request": "attach"
        }
    ]
}
