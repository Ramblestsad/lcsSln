apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-inventory-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-inventory-worker
  template:
    metadata:
      labels:
        app: todo-inventory-worker
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: todo-inventory-worker
          image: todo-inventory-worker:dev
          imagePullPolicy: IfNotPresent
          env:
            - name: DOTNET_ENVIRONMENT
              value: Production
            - name: ConnectionStrings__postgres
              valueFrom:
                secretKeyRef:
                  name: todo-inventory-config
                  key: postgres
            - name: RabbitMq__Host
              value: rabbitmq
            - name: RabbitMq__Username
              valueFrom:
                secretKeyRef:
                  name: todo-inventory-config
                  key: rabbitmq-username
            - name: RabbitMq__Password
              valueFrom:
                secretKeyRef:
                  name: todo-inventory-config
                  key: rabbitmq-password
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
